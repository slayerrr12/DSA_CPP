#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int hammingWeight(uint32_t n) {

        //using bit masking and also checking each bit
        for (int i = 0; i < ; i++)
        {
            /* code */
        }
        
        
    }
};